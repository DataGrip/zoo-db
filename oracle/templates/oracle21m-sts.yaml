apiVersion: v1
kind: Service
metadata:
  name: oracle21m
  labels:
    oracle: oracle21m
spec:
  ports:
    - port: 1521
      name: oracle
  {{/*clusterIP: None*/}}
  publishNotReadyAddresses: true
  selector:
    oracle: oracle21m
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "oracle.name" . }}-21m
  labels:
    oracle: oracle21m
  {{- include "oracle.labels" . | nindent 4 }}
spec:
  serviceName: oracle21m
  replicas: 1
  selector:
    matchLabels:
      oracle: oracle21m
    {{- include "oracle.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        oracle: oracle21m
      {{- include "oracle.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        fsGroup: 1000
      setHostnameAsFQDN: true
      imagePullSecrets:
      {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      containers:
      - name: oracle21m
        imagePullPolicy: {{ .Values.pullPolicy }}
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
        env:
        - name: ORACLE_PWD
          valueFrom:
            secretKeyRef:
              key: password
              name: {{ include "oracle.fullname" . }}-sys-password
        image: {{ .Values.oracle21m.image.repository }}:{{ .Values.oracle21m.image.tag | default
          .Chart.AppVersion }}
        ports:
        - containerPort: 1521
          protocol: TCP
        resources: {{- toYaml .Values.oracle21m.resources | nindent 10 }}
        volumeMounts:
        - mountPath: {{ .Values.scriptsMountPath.default }}
          name: oracle-scripts
      hostname: oracle21m
      volumes:
      - configMap:
            name: oracle-scripts
        name: oracle-scripts
